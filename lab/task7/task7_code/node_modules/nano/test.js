const Nano = require('.')

const nano = Nano({ url: process.env.COUCH_URL })

const main = async () => {
  const cities = nano.db.use('cities')
  const i = await cities.info()
  console.log(i)
}

main()
